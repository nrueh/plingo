sorts
#boolean={true,false}. 
#patch={p1,p2}.
#day=1..1.


attributes

hidden_in:#patch.
found:#day->#boolean.
look: #day->#patch.

statements



random(hidden_in).
random(found(D))  :- hidden_in=P, look(D)=P.
found(D)=false :- hidden_in = P1, look(D) = P2, P1!=P2.


pr(hidden_in=p1) = 8/10.
pr(found(D)) = 2/10.


%SECOND SCENARIO:
look(1)=p1.

obs(found(1), false).
%obs(found(5), false).

?found(1). 
